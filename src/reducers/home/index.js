

const initialState = [
  {
    text: 'Use Redux',
    completed: false,
    id: 0
  }
]

export default function home(state = initialState, action) {
  switch (action.type) {
    case "ADD_TODO":
    default:
      return state
  }
}